/* Abaixo comentado, um exemplo de codigo que está com erro e pararia
de funcionar ao ser executado. Pois estamos tentando 
somar as variaveis a e b, contudo, a e b não foram declaradas
*/
//console.log(a+b);
//console.log ('This line is never reached')

/*Abaixo um comando que pode gerar alertas de erros, 
ou seja, voce pode gerar seus proprios alertas de erro,
e melhorar o modo como resolve eles.*/

throw new ReferenceError('Testando meu novo erro');

