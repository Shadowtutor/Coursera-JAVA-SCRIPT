//Random integer
//Get a random decimal number btween 0 and 0.99..
//and save it to a variable
var decimal = Math.random()*10;

//Rounding up the value of decimal
var rounded = Math.ceil(decimal);

//log the value of rounded to the console
console.log(rounded);